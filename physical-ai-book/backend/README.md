
# Backend Server for Chatbot

This directory contains a simple Python Flask server to act as the backend for the chatbot.

## Setup and Running

1. **Install dependencies:**
   ```bash
   pip install Flask flask-cors
   ```

2. **Run the server:**
   ```bash
   python main.py
   ```

The server will start on `http://localhost:8000`.

		